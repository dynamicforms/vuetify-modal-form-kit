// import { App } from 'vue';

export * from './core';
export * from './layout';
// export * from './modal';

export const DynamicFormsModalFormKit = {
  install: (/* app: App */) => {
    // Object.entries(ModalComponents).map(([name, component]) => app.component(name, component));
  },
};
